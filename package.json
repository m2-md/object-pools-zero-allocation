{
  "name": "object-pools-zero-allocation",
  "version": "1.0.0",
  "description": "Canvas'ta nesne havuzları ve sıfır-ayırmalı oyun döngüsü — Pool<T>, havuzlu parçacık sistemi, in-place vektörler ve pooled vs unpooled benchmark",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
